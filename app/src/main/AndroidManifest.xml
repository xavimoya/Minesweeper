<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="xavi.minesweeper">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.SEND_SMS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Minessweeper_main"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Help" />
        <activity android:name=".Game" />
        <activity android:name=".Config" />
        <activity android:name=".Results" />
        <activity android:name=".Log" />
        <activity android:name=".Frag_game" />
        <activity android:name=".DisplayBBDD" />
        <activity android:name=".MySharedPreferences"></activity>
    </application>

</manifest>